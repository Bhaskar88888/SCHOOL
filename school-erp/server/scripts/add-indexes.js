/**
 * Index management is handled by Prisma migrations.
 * Use `npx prisma migrate deploy` or `npx prisma db push` as needed.
 */

console.log('Prisma manages database indexes via schema.prisma and migrations.');
console.log('Run: npx prisma migrate deploy');
process.exit(0);
